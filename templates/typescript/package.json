{
  "name": "typescript",
  "version": "1.0.0",
  "scripts": {
    "prepare": "husky install",
    "build": "yarn rimraf .dist/ && swc src/ -d .dist/",
    "build:watch": "nodemon"
  },
  "devDependencies": {
    "@commitlint/cli": "^16.2.3",
    "@commitlint/config-conventional": "^16.2.1",
    "@swc/cli": "^0.1.57",
    "@swc/core": "^1.2.162",
    "@types/node": "^17.0.23",
    "husky": "^7.0.4",
    "rimraf": "^3.0.2",
    "typescript": "^4.6.3"
  }
}
